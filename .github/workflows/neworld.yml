name: Neworld Sign in

on:
  workflow_dispatch:
    

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: print something
        run: env | pwd
  build:
    runs-on: ubuntu-latest
    steps:
      - name: ssh & make code run
        uses: peterkimzz/aws-ssm-send-command@master
        id: ssm
        with:
          aws-region: ap-east-1b
          aws-access-key-id: AKIA2JYANUWQMD7E36KX
          aws-secret-access-key: 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
          instance-ids: i-0dbe746bc4326295c

          working-directory: /home/ubuntu/code
          command: ls -al
            
          
